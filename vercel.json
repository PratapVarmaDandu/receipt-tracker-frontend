{
  "buildCommand": "npx ng build --configuration production",
  "outputDirectory": "dist/receipt-tracker-frontend/browser",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}

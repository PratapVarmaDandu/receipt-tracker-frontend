import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.securesteward.app',
  appName: 'Receipt Tracker',
  webDir: 'dist/receipt-tracker-frontend/browser'
};

export default config;
